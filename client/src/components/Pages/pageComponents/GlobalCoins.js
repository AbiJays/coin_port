const GlobalCoins = ({coinDataDaily}) => {

    if (coinDataDaily.length > 0) {

        // const globalCoinDataDisplay = coinDataDaily
        //     return (

        //     )
        return (
            <p>Global Coins</p>
        )
    }

}